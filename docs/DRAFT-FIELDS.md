# DRAFT fields

Generated by `node scripts/draft-fields.mjs` — do not edit by hand.

**15 unfilled** across 9 of 9 projects.

These are the `limitations` entries only Hamza can write. They are stripped
from the rendered case study (`src/app/work/[slug]/page.tsx`) and shown as a
dev-only banner, so nothing below is currently public.

Why they matter: the limitations block is the highest-signal section on a case
study for a senior reviewer. An empty one reads as inexperience, and a vague
one reads as evasion. Specific and technical is the bar — lighting, fabric,
occlusion, pose, drift, throughput ceiling.

---

## MagicQC

`magicqc` — 3 unfilled

- [ ] **projects.ts:46** — which fabrics, colours or drape conditions degrade the measurement, and by roughly how much.
- [ ] **projects.ts:47** — what the system requires of the capture rig (fixed height, backdrop, lighting) to hold accuracy.
- [ ] **projects.ts:48** — what happens on garment types outside the trained catalogue.

## AI-Enabled Fabric Defect Detection

`fabric-defect-detection` — 3 unfilled

- [ ] **projects.ts:99** — PatchCore anomaly scoring is sensitive to lighting and texture shifts not represented in the reference set - say what that cost you in false positives.
- [ ] **projects.ts:100** — which defect classes the supervised branch is weakest on, and the class imbalance behind it.
- [ ] **projects.ts:101** — the throughput ceiling on OAK-1W and the loom speed it implies.

## SkyResQ

`skyresq` — 2 unfilled

- [ ] **projects.ts:145** — altitude and ground-sample-distance band where detection holds, and where it falls apart.
- [ ] **projects.ts:146** — what simulation did not capture that real flight did.

## Dock Vision AI

`dock-vision-ai` — 1 unfilled

- [ ] **projects.ts:189** — weather, sea state and light conditions the system was not validated in.

## CV Recruiter RAG Agent

`cv-recruiter-rag` — 2 unfilled

- [ ] **projects.ts:231** — how the agent behaves on CVs in unusual layouts or scanned images.
- [ ] **projects.ts:232** — what you did about the obvious bias risk in automated candidate ranking - reviewers will ask.

## Real-Time PPE & Workplace Safety Detection

`ppe-safety-detection` — 1 unfilled

- [ ] **projects.ts:278** — camera placement and crowd-density conditions where detection degrades.

## Industrial Detection & Pose-Estimation Suite

`industrial-pose-suite` — 1 unfilled

- [ ] **projects.ts:319** — the accuracy you traded away to hit 15 FPS, and on which classes.

## Enterprise RAG & Conversational Agents

`enterprise-rag-agents` — 1 unfilled

- [ ] **projects.ts:359** — your actual grounding failure rate and what you did about it.

## SafePulse

`safepulse` — 1 unfilled

- [ ] **projects.ts:393** — what it does when the network drops - the exact case it exists for.
